# ECESSP ML Backend

Use `README_MVP.md` for local startup and troubleshooting.

Quick start:

```powershell
cd ecessp-ml
.\start_backend.ps1
```

Validation protocol:

- `reports/validation_protocol/README.md` for DFT + experiment confidence validation and acceptance gates.

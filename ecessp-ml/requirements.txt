# ======================================================
# ECESSP â€“ Phase 1 Requirements
# Dataset Validation & Preprocessing (READ-ONLY)
# ======================================================

python-dateutil>=2.8.2
pytz>=2023.3

numpy>=1.24.0
pandas>=2.0.0

# ------------------------------------------------------
# Notes:
# - No ML frameworks installed yet (PyTorch, DGL, etc.)
# - Added later during PI-GNN implementation phase
# - This ensures minimal, reproducible validation
# ------------------------------------------------------

# ------------------------------------------------------
# Runtime & API dependencies (lightweight)
# These allow running the FastAPI-based backend for health
# probes and basic functionality without heavy ML libs.
# Install optional ML frameworks separately when needed.
# ------------------------------------------------------
fastapi>=0.95.0
uvicorn[standard]>=0.20.0
pydantic>=1.10.7
python-multipart>=0.0.6
typing-extensions>=4.5.0
# Optional / heavy (install only when ready):
torch>=2.2.0
# dgl
# pytorch-lightning
# ------------------------------------------------------

matplotlib>=3.8.0

